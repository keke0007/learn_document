## 内容回顾

seaborn绘图

- import seaborn as sns

- sns.XXXplot(data,x,y,hue)

数据分析业务重要指标

- dau mau 次留 7留  gmv nmv 客单价 转化率/转发率   cpm/cpc

思维方法模型

- 多维度拆解 漏斗分析(路径规范, 环节相对较多) 分群  当数据有波动要把时间周期拉长,看是不是周期性的规律
- aipl  aaaa  aaaaa  用户生命周期 导入期  成长期  活跃 成熟 沉睡 流失

自动化报表

- sql pandas  excel

  - 数据来源是 hadoop hive / Mysql 优先选择sql 处理问题
  - 数据源如果是 excel  
    - pandas  (①数据涉及很多表, ② 重复操作)
    - excel

  - 团队的技术栈就是python (算法)

## 0 MatPlotLib画图

matplotlib功能是比较全

- plt.text 绘制文字内容 

## 1 RFM用户价值分群

RFM 体现的也是分群的思想, 但是是从用户价值角度进行分群 , 之前介绍的aipl aaaa, aaaaa 主要考虑的是用户生命周期

R recency (最后一次消费距今的时间间隔)              最近一周以内来过                     高    否则 低

F  frequency (最近一段时间内的消费次数)          最近半年  来过25次以上           高  否则 低

M monetary(最近一段时间内消费的总金额)          最近半年   消费3000元以上     高  否则 低

RFM适合落地的场景和几种使用方法

- 业务需要有消费, 消费的频率不能太低 , 典型的适用业务 电商, 外卖, 打车, 旅游
- 使用方法
  - RFM每个维度做2分  分8群
  - RFM每个维度做3分 分27群    3好,2,1差             333,321,313
  - RFM每个维度做5分  每个维度的分数计算平均值  用均值来做2分 分8群

RFM的变种 RFE

- 业务没有消费, 但用户的使用频率比较高
- E 活跃度    0.1* 浏览, 0.3*转  0.2 * 评  0.2*赞,  0.2 * 收藏

分群的目的:给不同的用户提供不同的有针对性的运营策略

数据分析的同学,在RFM模型实施过程中的工作

- 给用户贴上标签
- 定期的统计每部分用户的人数



### 1.2 代码实现

加载数据, 数据的清洗

- 使用用户消费流水数据, 用户ID, 订单ID,消费的时间, 消费的金额
- 空值删掉
- 异常值去掉

计算RFM的标签

- 首先要算出 R, F , M的原始值
  - R  用户ID分组 消费时间去最大max   当前的时间 - 最大的时间
  - F  用户ID分组对 订单ID count
  - M  用户ID分组对消费金额 sum
- 当前用每个维度打3分的方式来做 , 先确定阈值, 然后pd.cut 打分 
- 三个维度的标签拼接到一起

统计RFM每组的人数

数据可视化(pyecharts 3d柱状图 )

- pyecharts  用js实现 是百度的开源的开始画库echarts 的python封装

**加载数据**

```python
import pandas as pd
sheet_names = ['2015','2016','2017','2018']
sheet_datas = pd.read_excel('C:/Develop/深圳42/data/sales.xlsx', sheet_name=sheet_names)
#%%
for sheet_name in sheet_names:
    print(sheet_datas[sheet_name].head())
    print(sheet_datas[sheet_name].info())
    print(sheet_datas[sheet_name].describe())
    print('==============='+sheet_name+'===============')
```



通过数据查看之后， 发现了一些问题

- 数据有缺失 有两年的数据各少了一条
- 数据可能存在异常， 订单的金额最小 0.0 0.1 0.5 最大的十几万 这些需要跟业务方进行沟通 询问是否有问题

**数据预处理**

```python
# 数据从excel读取出来之后， 返回的是一个dict key是sheet_name  values 就是每一个工作簿的数据
data_merge = pd.concat(sheet_datas.values())
# 去缺失值 
data_merge.dropna(inplace=True)
# 保留订单金额>1
data_merge.query('订单金额>1', inplace=True)
# 提取年份  每一年单独计算RFM 2015 ~2018 4年的数据， 计算4个RFM
data_merge['年份'] =data_merge['提交日期'].dt.year
```



计算时间间隔, 这里以每一年最后一天为基准， 计算每一年的RFM标签

```python
# 按年份获取，每一年日期的最大值   
data_merge.groupby('年份')['提交日期'].agg(max) # 分组聚合， 聚合之后每一组只有一个结果
#%%
data_merge['计算日期'] = data_merge.groupby('年份')['提交日期'].transform('max')
#%%
# 计算消费日期（提交日期） 举例计算日期的时间间隔  
data_merge['购买日期差值'] = (data_merge['计算日期']-data_merge['提交日期']).dt.days
```

**计算RFM的聚合值**

```python
#%%
# 时间间隔 取最小值， 订单金额：sum  提交日期： count
rfm_result = data_merge.groupby(['年份','会员ID']).agg({'购买日期差值':'min','订单号':'count','订单金额':'sum'}).reset_index()
# 重命名一下列名
rfm_result.columns = ['年份', '会员ID', 'R', 'F', 'M']
```

**给RFM三个维度打分**

```python
## 确定RFM的划分区间
#%%
rfm_result.iloc[:,2:].describe()
#%%
# 27  R 1,2,3   F 1,2,3  M 1,2,3
r_bins= [-1,79,255,365]
f_bins = [0,2,5,130] # 和业务方讨论， 或者这个规则直接由业务方提供
m_bins= [0,69,1199,206252]

# labels=[1,2,3] 这种写法, 写死了叫硬编码, 如果有办法替换, 尽量避免,否则当业务发生变更每次需要修改代码
# range(开始的值, 结束的值, 步长) 步长默认是1
# range(len(r_bins)-1,0,-1)  从len(r_bins)-1开始  到0结束 每次-1   结束的值不会包含进来
rfm_result['r_score'] =pd.cut(rfm_result['R'],bins=r_bins,labels=[i for i in range(len(r_bins)-1,0,-1)])
rfm_result['f_score'] =pd.cut(rfm_result['F'],bins=f_bins,labels=[i for i in range(1,len(r_bins))])
rfm_result['m_score'] =pd.cut(rfm_result['M'],bins=m_bins,labels=[i for i in range(1,len(r_bins))])

#%%
# pd.cut之后得到的是 category这个类型， 这种类型不能直接拼接， 先转换成字符串 astype(str)
rfm_result['rfm_labels'] = rfm_result['r_score'].astype(str)+rfm_result['f_score'].astype(str)+rfm_result['m_score'].astype(str)

```

绘图数据准备

```python
display_data = rfm_result.groupby(['rfm_label','年份'],as_index=False)['会员ID'].agg('count')
display_data.columns=['rfm_label','year','number']
```



pyecharts绘图

```python
import pyecharts
pyecharts.__version__
#%%
# pip install pyecharts -i https://pypi.tuna.tsinghua.edu.cn/simple
#%%
# 显示图形
import pyecharts.options as opts
from pyecharts.charts import Bar3D # 3D柱形图

# 颜色池
range_color = ['#313695', '#4575b4', '#74add1', '#abd9e9', '#e0f3f8', '#ffffbf',
               '#fee090', '#fdae61', '#f46d43', '#d73027', '#a50026']

range_max = int(display_data['number'].max()) # 人数取最大值，左下角可以拖动的控制条， 最大值表示多少
c = (
    Bar3D()#设置了一个3D柱形图对象
    .add(
        "",#图例
        [d.tolist() for d in display_data.values],#数据
        xaxis3d_opts=opts.Axis3DOpts(type_="category", name='分组名称'),#x轴数据类型，名称，rfm_group
        yaxis3d_opts=opts.Axis3DOpts(type_="category", name='年份'),#y轴数据类型，名称，year
        zaxis3d_opts=opts.Axis3DOpts(type_="value", name='会员数量'),#z轴数据类型，名称，number
    )
    .set_global_opts( # 全局设置
        visualmap_opts=opts.VisualMapOpts(max_=range_max, range_color=range_color), #设置颜色，及不同取值对应的颜色
        title_opts=opts.TitleOpts(title="RFM分组结果"),#设置标题
    )
)
c.render('rfm.html') #在目录中生成一个HTML的页面
```

### 2.4 RFM小结

RFM体现了分群的思路， 将用户划分成不同的群体是很重要的精细化运营、精准营销的手段

RFM 最适合落地的业务就是电商

- 有一些业务 比如 信息流（微博，今日头条） 没有很多收费的地方但是用户使用的频率很高， 很多用户也不会产生消费， 这个时候可以使用RFM的变种， RFE E代表活跃 （浏览，收藏，发表评论， 转发，点赞）

RFM和计算的时间密切相关的

- 对于某个用户来说， 计算RFM的时间差了一天，可能会落到不同的组中
- RFM的值需要经常更新

## 2 AB测试

### 2.1 AB测试简介

产品设计, 功能, 算法进行调整的时候, 为了避免错误的调整损害用户体验, 往往在正式上线之前都要经历(大厂的主要产品)AB测试

- AB测试的目的, 让一小部分用户作为所有用户的代表来体验新的设计/功能/算法
- 如何确定参与实验的人数, 是AB测试的关键, 参与实验的人数太少, 实验结果可信度太低, 参与实验人数过多, 设计如果不尽人意, 影响会变大

AB测试的流程

- 明确KPI 当前的调整的目的是啥?  比如 通过调整头图, 提升当前产品的点击率/转化率
  - 当前的转化率是多少  12%
  - 目标转化率是多少   15%

- 计算参与实验的人数
- 给不同的人贴上对应的标签(对照组, 实验组), 开始实验
  - 选对照组  使用的是老的策略， 看到的是老的设计 避免在进行实验的过程中，外部条件的变化导致关键指标的波动
  - 选测试组  使用新的策略，看到的是新的设计
- 在实验期间, 观察数据的情况, 回收数据
- 使用回收的数据计算KPI是否达到要求
  - 达到要求, 说明新的设计是可以上线的
  - 如果没有达到要求
    - 如果差的不多, 可以多放一些样本进来重新计算
    - 如果差的比较多, 新的设计不能上线

### 2.2 AB测试的辛普森悖论

辛普森悖论为英国统计学家E.H.辛普森于1951年提出，即在某个条件下的两组数据，分别讨论时都会满足某种性质，可是一旦合并考虑，却可能导致相反的结论。

AB测试中产生辛普森悖论的原因：流量分割不均匀导致的实验组与对照组的用户特征不一致

AB测试中如何避免辛普森悖论

- 要得到科学可信的 AB 测试试验结果，就必须合理的进行正确的流量分割，保证试验组和对照组里的用户特征是一致的，并且都具有代表性，可以代表总体用户特征。

### 2.3 AB测试案例

**计算参与实验人数**

```python
import pandas as pd
import statsmodels.stats.api as sms
from math import ceil
#%% md
## 计算参与实验人数
#%%
# 转化率当前是13%  我们要提升到15%
effect_size = sms.proportion_effectsize(0.13,0.15)
# 计算参与实验人数  power 统计功效 一般选择0.8   alpha 显著性水平 一般取5%
required_n = sms.NormalIndPower().solve_power(effect_size,power=0.8,alpha=0.05,ratio=1)
required_n = ceil(required_n)
required_n
```

**加载实验结果数据计算**

```python
ab_data = pd.read_csv('C:/Develop/深圳42/data/ab_data.csv')
ab_data['group'].value_counts()
ab_data['landing_page'].value_counts()
```

**去掉同时看过老页面和新页面的用户**

```python
session_counts = ab_data['user_id'].value_counts()
users = session_counts[session_counts<2].index
ab_data=ab_data[ab_data['user_id'].isin(users)]
```

**筛选满足实验条件的人数, 计算转化率**

```python
# 从数据中采样 4720个
control_sample = ab_data[ab_data['group']=='control'].sample(n=required_n,random_state=13)#对照组看老页面
test_sample = ab_data[ab_data['group']=='treatment'].sample(n=required_n,random_state=13)#实验组 看新页面
control_sample['converted'].mean() # 计算控制组转化率  购买人数/总访问人数
# 计算实验组转换率
test_sample['converted'].mean()
```

